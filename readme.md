# WebTransport 

WebTransport implementation with minimal HTTP3 underpinnings. 

It has two crates, `wt-proto` & `wt-rs` ,

`wt-proto` - Contains protocol logic and parsing and HTTP3 stuff.
`wt-rs` - Actual Implementation of WebTransport. 
